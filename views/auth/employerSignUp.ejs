<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;700&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/stylesheets/bootstrap.css" />
    <link rel="stylesheet" href="/stylesheets/employer-sign-up.css" />
    <title>Talent Haven | <%= pageName %></title>
    <style>
      form .invalid {
        border: 1px solid palevioletred !important;
      }
    </style>
  </head>

  <body>
    <nav class="navbar navbar-expand-sm">
      <a class="navbar-brand prj-link" href="#">
        <span class="prj-text-primary">Talent </span>
        <span class="prj-text-accent">Haven </span>
      </a>
      <button
        class="navbar-toggler"
        type="button"
        data-toggle="collapse"
        data-target="#navbarNav"
        aria-controls="navbarNav"
        aria-expanded="false"
        aria-label="Toggle navigation"
      >
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav mx-auto">
          <li class="nav-item">
            <a class="nav-link prj-header-link font-weight-bold" href="#"
              >HEADER GOES HERE<span class="sr-only">(current)</span></a
            >
          </li>
        </ul>
      </div>
    </nav>

    <main>
      <div class="container-fluid main-container my-auto">
        <div class="row py-5">
          <div class="col-md-5 mx-auto my-auto d-none d-md-block">
            <div
              class="d-flex flex-column justify-content-center align-items-center"
            >
              <h1>
                <span class="prj-text-primary">Talent</span>
                <span class="prj-text-accent">Haven</span>
              </h1>
              <p class="text-center">
                <a href="#" class="prj-link">Sign in</a> or
                <a href="#" class="prj-link">Create an account</a>
              </p>
            </div>
          </div>
          <section
            class="form-container shadow col-10 px-4 col-sm-8 col-md-6 px-md-4 col-lg-5 px-lg-5 mx-auto col-xl-4 bg-white br-5"
          >
            <h1 class="prj-header-h4 font-weight-bold text-center py-3">
              Sign up
            </h1>
            <% if (errorMessage) { %>
            <div
              role="alert"
              class="alert alert-danger alert-dismissible fade show deposit-alert"
            >
              <button type="button" class="close" data-dismiss="alert">
                &times;</button
              ><%= errorMessage %>
            </div>
            <% }if (success && success.length > 0) { %>
            <div role="alert" class="alert alert-success user-message">
              <%= success %>
            </div>
            <% } %>

            <form
              name="employerSignUpForm"
              action="/employer/register"
              method="POST"
            >
            
              <div class="row">
                <div class="form-group col-12 col-md">
                  <label for="firstName" class="prj-normal-text"
                    >First name</label
                  >
                  <input
                    type="text"
                    name="firstName"
                    class="form-control prj-form-control <%= validationErrors.find(e => e.param === 'firstName') ? 'invalid' : '' %>"
                    id="firstName"
                    placeholder="First name"
                    autocapitalize="on"
                    autocomplete="given-name"
                    minlength="2"
                    value="<%= oldInput.firstName %>"
                  />
                </div>
                <div class="form-group col-12 col-md">
                  <label for="lastName" class="prj-normal-text"
                    >Last name</label
                  >
                  <input
                    type="text"
                    name="lastName"
                    class="form-control prj-form-control <%= validationErrors.find(e => e.param === 'lastName') ? 'invalid' : '' %>"
                    id="lastName"
                    placeholder="Last name"
                    autocapitalize="on"
                    autocomplete="family-name"
                    minlength="2"
                    value="<%= oldInput.lastName %>"
                  />
                </div>
              </div>
              <div class="row">
                <div class="form-group col">
                  <label for="email" class="prj-normal-text"
                    >Email address</label
                  >
                  <input
                    name="email"
                    class="form-control prj-form-control <%= validationErrors.find(e => e.param === 'email') ? 'invalid' : '' %>"
                    id="email"
                    placeholder="Email address"
                    autocomplete="email"
                    value="<%= oldInput.email %>"
                  />
                </div>
              </div>

              <div class="row">
                <div class="form-group col">
                  <label for="password" class="prj-normal-text">Password</label>
                  <input
                    type="password"
                    name="password"
                    class="form-control prj-form-control <%= validationErrors.find(e => e.param === 'password') ? 'invalid' : '' %>"
                    id="password"
                    placeholder="Password"
                    autocomplete="new-password"
                    value="<%= oldInput.password %>"
                  />
                </div>
                <%= csrfToken %>
                <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
              </div>
              <div class="row pt-2 pb-3">
                <div class="col">
                  <p class="sign-up-alt-text text-center mb-0">
                    or sign up with
                  </p>
                </div>
              </div>
              <div class="row pb-3">
                <div class="col d-flex justify-content-center">
                  <button
                    class="btn shadow-sm mr-2 d-flex justify-content-around align-items-center"
                  >
                    <img
                      class="img-fluid btn-img mr-2"
                      src="images/employer-sign-up/icon-google.svg"
                      alt="github button image"
                    />
                    <span class="mb-0 prj-normal-text">Google</span>
                  </button>
                  <button
                    class="btn shadow-sm ml-2 d-flex justify-content-around align-items-center"
                  >
                    <img
                      class="img-fluid btn-img mr-2"
                      src="images/employer-sign-up/icon-github.svg"
                      alt="github button image"
                    />
                    <span class="mb-0 prj-normal-text">Github</span>
                  </button>
                </div>
              </div>
              <div class="row">
                <div class="col">
                  <div class="form-check pb-3">
                    <input
                      class="form-check-input"
                      type="checkbox"
                      name="isTocChecked"
                      id="isTocChecked"
                      value="true"
                      required
                    />
                    <label
                      class="form-check-label prj-normal-text"
                      for="isTocChecked"
                      >By clicking, you are agree to our
                      <a href="#" class="prj-link prj-normal-text"
                        >Terms and Conditions</a
                      ></label
                    >
                  </div>
                  <div
                    class="d-flex flex-column align-items-center justify-content-between"
                  >
                    <button
                      type="submit"
                      class="btn btn-lg prj-btn-primary br-5"
                    >
                      Create account
                    </button>
                    <p class="prj-normal-text pt-3">
                      Already have an account?
                      <a href="#" class="text-center prj-link">Sign in</a>
                    </p>
                  </div>
                </div>
              </div>
            </form>
          </section>
        </div>
      </div>
    </main>

    <footer></footer>

    <script src="/javascripts/jquery.js"></script>
    <script src="/javascripts/bootstrap.js"></script>
    <script src="/javascripts/popper.js"></script>
    <!-- <script src="/javascripts/employer-sign-up.js"></script> -->
  </body>
</html>
